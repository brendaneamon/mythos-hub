<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Mythos Hub</title>
    <link rel="stylesheet" href="http://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.6/css/materialize.min.css">
    <link rel="stylesheet" href="/assets/css/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <base href="/"></base>
  </head>
  <body>
    <header>
      <nav class="darken 2">
        <div class="nav-wrapper container">
          <ul>
            {{#if user}}
            <li><img class="circle responsive-img z-depth-2" src="{{user.t_photo_url}}" alt="{{user.name}}" /></li>
            <li>Welcome, {{user.name}}!</li>
            <li><a href="/logout" class="brand-logo">Sign out</a></li>
            {{else}}
            <li><a href="/login/twitter" class="brand-logo">Sign in</a></li>
            {{/if}}
          </ul>
        </div>
      </nav>
    </header>
    {{{body}}}
  </body>
  </html>
